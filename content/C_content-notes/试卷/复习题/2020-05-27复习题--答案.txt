脱敏文本版：2020-05-27复习题--答案.doc
原始 Word/PDF 附件已移除，避免公开仓库保留学校、公司、个人或文档作者元数据。

2020-05-27复习题--答案
1、下面的程序输出的结果是 程序出错无a[3]       ？
    #include <stdio.h>
    int main (void)
{
        int a =100;
        int b[3]={200,300,400};
        Int c=500;
        printf(“%d\n”, a[3]);
        return 0;
}

2、写出下面程序打印的结果？
void main(void)
{
    int a[5]={1, 2, 3, 4, 5};
    Int *ptr=(int *)(&a+1);
    printf(”%d, %d”, *(a+1), *(ptr-1));
}
24
3、写出字符串strcpy的函数实现方程式.
char * mystrcpy(char * str1, const char * str2)
{
    int i=0;
    while(str2[i]!=&apos;\0&apos;)
    {
        *(str1+i) =*(str2+i);
        i++;
    }
    str1[i]=str2[i];
    return str1;
}

题目不在多，而在于你需要有计划的复习
