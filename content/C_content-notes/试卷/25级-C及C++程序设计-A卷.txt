脱敏文本版：25级-C及C++程序设计-A卷.doc
原始 Word/PDF 附件已移除，避免公开仓库保留学校、公司、个人或文档作者元数据。

《C/C++程序设计》课程 期末 考试 A 试卷
考试班级      专业B25201班         考试形式  闭卷   考试时间  120  分钟
考试学期  2025-2026学年第1学期    考试类型  考试     命题教师   虚拟教师甲

题 号一二三四五六总分分 值2010103030100
一、单项选择题（每小题1分，共20分)
1、一个 C 程序的执行是从（ ）。
A、本程序的 main 函数开始，到 main 函数结束
B、本程序的第一个函数开始，到本程序文件的最后一个函数结束
C、本程序的 main 函数开始，到本程序文件的最后一个函数结束
D、本程序的第一个函数开始，到本程序 main 函数结束
2、以下关于 C 语言标识符的说法，错误的是（ ）。
A、标识符由字母、数字、下划线组成		B、标识符不能以数字开头
C、标识符可以是 C 语言关键字			D、标识符区分大小写
3、若有定义int a[10];，则对 a 数组元素的正确引用是（ ）。
A、a [10]      B、a [3.5]     C、a (5、     D、a [10-10]
4、逻辑运算符两侧运算对象的数据类型（ ）。
A、只能是 0 或 1				B、只能是 0 或非 0 正数
C、只能是整型或字符型数据	D、可以是任何类型的数据
5、以下能正确定义二维实型数组 a 的选项是（ ）。
A、float a [3][4]; float a [ ][4];			B、float a (3,4); float a [3][4];
C、float a [3][4]; static float a [ ][4]={0};	D、float a [3][4]; float a [3][ ];
6、C 语言中，简单变量作为实参时，与对应形参的数据传递方式是（ ）。
A、地址传递									B、单向值传递
C、由实参传给形参，再由形参传回给实参		D、由用户指定传递方式
7、以下关于宏替换的叙述，正确的是（ ）。
A、宏名必须用大写字母表示		B、宏替换时要进行语法检查
C、宏替换不占用运行时间			D、宏定义中不允许引用已有的宏名
8、若有定义char *s = "ta\018bc";，则 for 循环for(;*s!='\0';s++)printf("*");的执行次数是（ ）。
A、9		B、5		C、6		D、7
9、设有结构体定义struct stu{int a;float b;}stutype;，以下叙述不正确的是（ ）。
A、struct 是结构体类型的关键字	B、struct stu 是用户定义的结构体类型名
C、stutype 是用户定义的结构体类型名		D、a 和 b 都是结构体成员名
10、在位运算中，操作数每右移一位，其结果相当于（ ）。
A、操作数乘以 2		B、操作数除以 2
C、操作数除以 4		D、操作数乘以 4
11、函数 ftell (fp、的作用是（ ）。
A、得到流式文件中的当前位置		B、移动流式文件的位置指针
C、初始化流式文件的位置指针		D、关闭流式文件
12、以下 do-while 循环的运行结果是（ ）。
int x=3; do {printf ("% d",x-=2);} while (!(--x));
A、输出 1		B、输出 1 和 - 2		C、输出 3 和 0	D、死循环
13、以下关于枚举类型的说法，正确的是（ ）。
A、枚举类型的取值必须是整数			B、枚举元素的值可以重复
C、枚举类型的定义必须多个元素   D、枚举元素的默认值从 0 开始依次递增
14、若有共用体定义union pw {int i; char ch[2];} a; a.ch[0] = 13; a.ch[1] = 0;，则 printf ("% d",a.i、的输出结果是（ ）。
A、13   	B、14	  C、208	   D、209
15、若要将文件位置指针移到距离文件末尾 20 个字节处，正确的 fseek 函数调用是（ ）。
A、fseek (fp, 20L, 0);		B、fseek (fp, 20L, 1);
C、fseek (fp, -20L, 2);		D、fseek (fp, 20L, 2);
16、以下关于 C 语言文件存取方式的说法，正确的是（ ）。
A、可以用文本方式存取，也可以用二进制方式存取
B、只能用文本方式存取
C、只能用二进制方式存取
D、文本方式和二进制方式不能混合使用
17、以下对 typedef 的叙述，不正确的是（ ）。
A、用 typedef 可以定义各种类型名，但不能用来定义变量
B、用 typedef 可以增加新类型
C、用 typedef 只是将已存在的类型用一个新的标识符来代表
D、使用 typedef 有利于程序的通用和移植
18、若有定义int *p[4];，则标识符 p 是（ ）。
A、一个指向整型变量的指针			B、一个指针数组名
C、一个指针，它指向一个含有四个整型元素的一维数组	D、定义不合法
19、以下关于位运算的说法，正确的是（ ）。
A、按位与运算可以用来清 0 特定位	B、按位或运算可以用来清 0 特定位
C、按位异或运算可以用来置 1 特定位	D、按位取反运算的结果一定是负数
20、以下关于 C 语言函数的说法，错误的是（ ）。
A、函数可以没有返回值		B、函数可以有多个参数
C、函数可以嵌套调用			D、函数可以嵌套定义
二、判断题（每小题1分，共10分）
1、注释说明只能位于一条语句的后面。（ ）
2、运算符 “%” 两侧必须是整型数据。（ ）
3、字符数组可以用赋值语句直接赋值字符串。（ ）
4、全局变量未初始化时默认值为 0。（ ）
5、带参宏的参数替换是在编译阶段进行的。（ ）
6、共用体变量的内存是成员中占内存最大者所需容量。（ ）
7、右移运算符 “>>” 等价于操作数除以 2。（ ）
8、文件存取以字节为单位的称为流式文件。（ ）
9、指向函数的指针可以调用对应的函数。（ ）
10、枚举类型的变量不能进行赋值运算。（ ）
三、填空题（每空1分，共10分）
1、C 语言的源程序必须通过【】和连接后，才能被计算机执行。
2、结构化程序由顺序结构、选择结构、【】 3 种基本结构组成。
3、若采用十进制数的表示形式，则 0xab 为【】。
4、运算符 % 两侧运算量的数据类型必须都是【】；运算符 ++ 和 -- 的运算量必须是变量。
5、若有定义语句：int m=5, y=2; 则执行表达式 y+=y-=m *=y 后的 y 值是【】。
6、已知字母 a 的 ASCII 码为十进制数 97，且设 ch 为字符型变量，则表达式 ch='a'+'8'-'3' 的值为【】。
7、若有定义：double x [3][5]; 则 x 数组中行下标的下限为0，列下标的上限为【】。
8、函数调用语句 fseek (fp, -20L, 2); 的含义是【】。
9、在 C 语言中，& 运算符作为单目运算符时表示的是地址运算；作为双目运算符时表示的是【】。
10、若有定义：union data {int i; char c; float f;} a; 则对共用体成员的正确赋值语句是【】。
四、程序阅读题（每题5分，共30分）
1．程序输出结果是什么？
#include<stdio.h>
int main(){
    char c[]="abcde";
    char *p=c;
    p+=2;
    printf("%s\n",p);
    return 0;
}答：
2．程序输出结果是什么？
#include<stdio.h>
int fun(int x,int y){
    return(y-x)*x;
}
int main(){
    int a=3,b=4,c=5,d;
    d=fun(fun(a,c),fun(a,b)+fun(c,b));
    printf("%d\n",d);return 0;
}答：
3．程序输出结果是什么？
#include<stdio.h>
#define MIN(x,y) (x)<(y)?(x):(y)
int main(){
    int i=10,j=15,k;
    k=10*MIN(i,j);
    printf("%d\n",k);
    return 0;
}答：
4．程序输出结果是什么？
#include<stdio.h>
int main(){
    int x=1,y=2;
    x=x^y;
    y=y^x;
    x=x^y;
    printf("%d,%d\n",x,y);
    return 0;
}答：
5．程序输出结果是什么？
#include<stdio.h>
int main(){
    FILE *fp;
    fp=fopen("test.txt","w");
    fprintf(fp,"%d,%c",10,'A');
    fclose(fp);
    fp=fopen("test.txt","r");
    int a;char b;
    fscanf(fp,"%d,%c",&a,&b);
    printf("%d,%c\n",a,b);
    fclose(fp);
    return 0;
}答：
6．程序输出结果是什么？
#include<stdio.h>
void swap(int *p1,int *p2){
    int temp=*p1;
    *p1=*p2;
    *p2=temp;
}
int main(){
    int x=5,y=7;
    swap(&x,&y);
    printf("%d,%d\n",x,y);
    return 0;
}答：




五、编程题（每题15分，共30分）

1、题目：编写函数 isprime (int a)，判断整型数 a 是否为素数，若是返回 1，否则返回 0，在主函数中调用该函数并输出结果。

2、题目：计算结构体数组中学生平均分
要求：定义struct Student（含学号、姓名、成绩），输入 3 名学生数据，计算平均分并输出。







 SHAPE \* MERGEFORMAT

 SHAPE \* MERGEFORMAT






封

第  PAGE  \* MERGEFORMAT 2 页     共  NUMPAGES  \* MERGEFORMAT 3 页

线

第  PAGE  \* MERGEFORMAT 3 页    共  NUMPAGES  \* MERGEFORMAT 3 页
