脱敏文本版：2020-05-08复习题--答案.doc
原始 Word/PDF 附件已移除，避免公开仓库保留学校、公司、个人或文档作者元数据。

2020-05-08复习题
1、 当c的值不为0时，在下列选项中能正确将c的值赋给变量a、b的是______。
 A、c=b=a; B、(a=c)‖(b=c); C、(a=c)&&(b=c); D、a=c=b;

答案：C

2、
struct stu
{ int x ;
int *y;
} *p ;
int dt[4]={ 10 , 20 , 30 , 40 };
struct stu a[4]={50 , &dt[0] , 60 , &dt[1] , 70 , &dt[2] , 80 , &dt[3] };
main()
{ p=a;
  printf("%d," , ++p->x); //语句1
  printf("%d," , (++p)->x ); //语句2
  printf("%d\n" , ++(*p->y) ); //语句3
}
输出的结果是：
A.10 , 20 , 20 B.50 , 60 , 21 C.51 , 60 , 21 D 60 , 70 , 31

答案：C
解析：这类题考察了结构体成员变量的访问以及运算符的优先级。需要注意的是“à”的优先级大于“++”和“* ”的优先级。
　　语句1中，先执行p->x ，得到50后再++ , 得到结果51。
　　语句2中，先执行(++P) ，指针移动后，取得成员变量x的值为60。
　　语句3中，先取得*p->y的值，即dt[1]的值，然后++ ，最后得21。

下面程序的功能是按学生的姓名查询其成绩排名和平均成绩。查询时可连续进行，直到输入0时才结束。
参考代码(所谓参考代码就是说可以参考里面正确的思路)：
#include <stdio.h>
#include <string.h>
#define NUM 4
struct student
{
int rank；
char *name；
float score；
}；
 struct student stu[ ]={ 3，"liming"，89.3，
4，"zhanghua"，78.2，
1，"anli"，95.1，
2，"wangqi"，90.6
}；
main()
{
char str[10]；
int i；
    do
{
printf("Enter a name")；
scanf("%s"，str)；
for( i=0；i<NUM；i++ )
if( strcmp(stu[i].name，str)==0 )
{
printf("Name :%8s\n"，stu[i].name)；
    printf("Rank :%3d\n"，stu[i].rank)；
    printf("Average :%5.1f\n"，stu[i].score)；
    break ；
}
if( i>=NUM ) printf("Not found\n")；
}while( strcmp(str，"0")!=0 )；
}



题目不在多，而在于你需要有计划的复习
