脱敏文本版：虚拟嵌入式培训机构第一阶段预科C试卷.pdf
原始 Word/PDF 附件已移除，避免公开仓库保留学校、公司、个人或文档作者元数据。

虚拟嵌入式培训机构 六、假如有数组a[4][5],请写出以下表达式的含义：
&a[1][0]:
《预科 C》试卷
a[3]:
&a[0]+1:
班级： 姓名：
*(&a[1][0]+1):
一、char str[] = “Hello” ; char *p = str ;
strlen(str) = ________ sizeof(p) = ________
七、请定义一个字符指针数组，该数组中有4个元素
sizeof(str) = ________
二、有数组int a[10],编写表达式求数组a的大小(数组占用字节数) 八、请编写程序对第七题所定义的指针数组的第一个元素进行内存分配，
分配的空间大小为100 bytes
三、有数组int a[10],编写表达式求数组a的长度(数组中元素的个数)
九、请定义一个整型数组指针p，该指针指向的数组长度为5
四、有二维数组int a[3][4],将数组a的第2行第3列的元素赋值为10
（行号和列号从0开始）
十、假如有二维数组a[3][5],请编写代码实现：将第九题定义的数组指针
p指向二维数组a，并且通过指针p将数组a的第1行第2列的元素赋值
五、typedef int* (*FUNC)(int,int);表示什么含义？
为100（行号和列号从0开始）
第 1 页 共 4 页
十一、嵌入式系统总是要用户对变量或寄存器进行位操作。给定一个整型 十三、、有两个字节chara,b;a=0x01,b=0x02,a 和b 分别为一个short
变量a，写两段代码，第一个设置a 的bit3，第二个清除a 的bit3。在以 类型数据val的高八位和低八位，在小端模式下，请编写出将a 和b 合并
上两个操作中，要保持其它位不变。 成Val 的表达式
十二、请问以下代码的执行结果是什么？为什么？ 十四、typedef在C 语言中频繁用以声明一个已经存在的数据类型的同义
字。也可以用预处理器做类似的事。例如，思考下面例子：
voidgetmemory(char*p)
{ #definedPSstructs*
p=(char*)malloc(100); typedefstructs*tPS;
strcpy(p,”helloworld”); 假如定义如下变量：
} dPSp1,p2; tPSp3,p4;
intmain()
请问p1、p2、p3、p4分别为什么类型的变量？
{ 用#define 和 typedef哪种方式更好？
char*str=NULL;
getmemory(str);
printf(“%s/n”,str);
free(str);
return0;
}
第 2 页 共 4 页
十五、请编写代码判断一个处理器是大端模式还是小端模式 十七、请问以下代码在执行的时候会出现什么问题？为什么？
voidwap(int*p1,int*p2){
int*p;
*p=*p1;
*p1=*p2;
*p2=*p;
}
voidmain(){
intx=5,y=10;
wap(&x,&y);
}
十八、下面的代码输出是什么，为什么？
十六、请问以下代码的执行结果是什么？为什么？
voidfoo(void)
char*s="AAA";
{
printf("%s",s);
unsignedinta=6;
s[0]='B'; intb=-20;
printf("%s",s); (a+b>6) ？printf(">6\n"):printf("<=6\n");
}
第 3 页 共 4 页
十九、请编写程序实现strncpy函数, 二十、请使用二分插入排序算法，编写函数实现将一个整数inta 插入到一
函数原型为：char*strncpy(char*dest,char*src,size_tn); 个升序数组intb[6]中，插入后保持数组b 依然为升序。
第 4 页 共 4 页
